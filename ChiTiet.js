function addToCart() {
    var quantity = document.getElementById("quantity").value;
    alert("Added " + quantity + " item(s) to the cart.");
    // You can add more functionality here, such as updating the cart in the backend.
}
